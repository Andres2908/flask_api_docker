version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install --upgrade pip
      - pip3 install gunicorn -r requirements.txt  # Asegúrate de incluir gunicorn
run:
  command: gunicorn -b 0.0.0.0:5000 app:app
  network: 
    port: 5000
